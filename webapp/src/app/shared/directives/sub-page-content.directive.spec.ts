import { SubPageContentDirective } from './sub-page-content.directive';

describe('SubPageContentDirective', () => {
  it('should create an instance', () => {
    const directive = new SubPageContentDirective();
    expect(directive).toBeTruthy();
  });
});
