import { SwitchActionableDirective } from './switch-actionable.directive';

describe('SwitchActionableDirective', () => {
  it('should create an instance', () => {
    const directive = new SwitchActionableDirective();
    expect(directive).toBeTruthy();
  });
});
